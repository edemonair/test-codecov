coverage:
  status:
    project:
      default: false
      platform:
        target: 20%
        threshold: 0%
        paths:
          - "app/"
    patch:
      default:
        threshold: 0%

comment:
  require_changes: true